import {TextInput as NativeInput} from 'react-native';
// import {useTheme} from 'styled-components/native';

export function TextInput() {

  return <NativeInput style={{fontFamily: 'Poppins'}} />;
}
